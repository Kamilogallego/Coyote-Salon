# --- Local (Docker Compose) ---
DB_HOST=localhost
DB_PORT=5433
DB_USER=coyote_admin
DB_PASSWORD=coyote123
DB_NAME=coyote_salon

# --- Producción (Render + Neon) ---
# Si DATABASE_URL está definida, tiene prioridad sobre las variables DB_* de arriba.
# Se obtiene del panel de Neon: Connection string. Usa sslmode=verify-full
# (no "require": son equivalentes hoy, pero "require" quedará obsoleto).
# DATABASE_URL=postgres://usuario:password@host.neon.tech/coyote_salon?sslmode=verify-full
# NODE_ENV=production

PORT=3000
SESSION_SECRET=cambia-este-secreto-en-produccion-por-uno-aleatorio

ADMIN_USERNAME=admin
ADMIN_PASSWORD=cambia-esta-contrasena
